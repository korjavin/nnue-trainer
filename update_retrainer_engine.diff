<<<<<<< SEARCH
    // Generate self-play dataset
    SelfPlayGenerator.Config spConfig = new SelfPlayGenerator.Config();
    spConfig.numGames = Math.max(10, config.minExamples() / 40); // Rough estimate for examples per game
    SelfPlayGenerator.GenerationResult spResult = SelfPlayGenerator.generate(spConfig, liveSearchEngine);
    System.out.println("Generated " + spResult.dataset.size() + " self-play examples. Distinct ratio: " + spResult.distinctGameRatio);

    java.util.List<NNUETrainer.TrainingExample> combinedExamples = new java.util.ArrayList<>(importResult.examples());
    for (SelfPlayGenerator.TrainingRecord record : spResult.dataset) {
      combinedExamples.add(new NNUETrainer.TrainingExample(record.features, record.target));
    }
=======
    // Generate self-play dataset
    SelfPlayGenerator.Config spConfig = new SelfPlayGenerator.Config();
    spConfig.numGames = Math.max(10, config.minExamples() / 40); // Rough estimate for examples per game
    spConfig.searchDepth = config.searchDepth();
    spConfig.seed = config.seed();
    SearchEngine isolatedEngine = new SearchEngine(liveSearchEngine.getNnueModel());
    SelfPlayGenerator.GenerationResult spResult = SelfPlayGenerator.generate(spConfig, isolatedEngine);
    System.out.println("Generated " + spResult.dataset.size() + " self-play examples. Distinct ratio: " + spResult.distinctGameRatio);

    java.util.List<NNUETrainer.TrainingExample> combinedExamples = new java.util.ArrayList<>(importResult.examples());
    for (SelfPlayGenerator.TrainingRecord record : spResult.dataset) {
      combinedExamples.add(new NNUETrainer.TrainingExample(record.features, record.target));
    }
>>>>>>> REPLACE
