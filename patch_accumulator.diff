<<<<<<< SEARCH
    for (int i = 0; i < 256; i++) {
      accum[i] -= hiddenWeights[i][cellIndex * 6 + oldState];
      accum[i] += hiddenWeights[i][cellIndex * 6 + newState];
    }
=======
    for (int i = 0; i < 256; i++) {
      if (oldState != 0) {
        accum[i] -= hiddenWeights[i][cellIndex * 6 + oldState];
      }
      if (newState != 0) {
        accum[i] += hiddenWeights[i][cellIndex * 6 + newState];
      }
    }
>>>>>>> REPLACE
