# WebSocket Bot Client Connection

Implement the WebSocket connection manager in the package `com.engine.nnue_trainer.protocol`.

## Tasks

- [ ] Task 1: Implement BotWebSocketClient

### Task 1: Implement BotWebSocketClient
1. Create a `BotWebSocketClient` class using Java-WebSocket `WebSocketClient`.
2. Implement methods to connect, disconnect, and handle WebSocket connection lifecycle (onOpen, onClose, onError).
3. Connect to the URL provided (defaults to `ws://localhost:8080/ws`).\n