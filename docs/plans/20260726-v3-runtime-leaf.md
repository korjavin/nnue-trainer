# NNUE v3.1b — runtime path: NNUEv3Accumulator + NNUEv3Evaluator + EVAL=NNUEV3 GoBot leaf

## Overview

Bead `nnue-trainer-aov`. Make the engine able to actually *evaluate* with the v3 features whose
capacity was just proven (`d4a.6.1`, held-out R² ≈ 0.94–0.98, merged in #94).

This is the last piece before a strength number: it turns `nnue_v3_weights.json` into a leaf
evaluation the GoBot search can call, so `d4a.6.2` can run the gauntlet against GoBot and against
the hand-tuned clone.

```
eval_v3(board, stm) = bias + Σ_{f active} weight_f      // exactly 144 active features
idx(r, c, state)    = (r * 12 + c) * 8 + state          // 1152 dense slots
```

**Why this is much simpler than the v2 equivalent it mirrors:**

- **No dictionary.** v2 needed a mined `PatternDictionary`, signature strings, hash lookups, a
  promotion threshold and `-1` miss handling. v3 indexes a dense array directly. None of that
  machinery is needed or wanted here.
- **No score scale.** v2 output a WDL-ish scalar and needed `NNUEV2_SCALE` to map it into score
  units — an entire bead (`d4a.4.1`) went into sweeping it. v3 was ridge-fit **against hand-tuned
  eval values**, so its output is already in hand-tuned score units (holdout MAE 1230 on a ±30000
  label). **Do not add a scale knob.** If one ever seems necessary, that is a signal something is
  wrong with the fit, not a knob to tune.
- **Full recompute is fine.** 144 array reads and adds per evaluation. Do NOT build incremental
  updates in this bead; add them only if a benchmark shows the leaf is the bottleneck.

## Context (from discovery)

Branch: `v3-runtime-leaf`, off `master`. **Merge target: master.**

- `nnue_v3_weights.json` (repo root, merged in #94) — `meta` carries `bias`, `lambda`, `top_n`,
  `n_features_total`, `r2_holdout`, corpus provenance (`games_used`, `positions_total`, …), and
  `weights` maps `"<feature_index>" -> weight` for all 1152 slots.
- `src/main/java/com/engine/nnue_trainer/train/V3FeatureMiner.java` — owns the canonical index:
  `BOARD = 12`, `STATES = 8`, `idx(r,c,state) = (r*BOARD + c)*STATES + state`. **Reuse this
  definition; do not restate the arithmetic in a second place.** Extract it if that is cleanest.
- `src/main/java/com/engine/nnue_trainer/v2/PatternContract.java` — `getSymbol(Cell, int stmOwner)`
  returns the perspective-normalized state (`EMPTY=0 … FORTIFIED_OPPONENT=7`; `OUT_OF_BOUNDS=8`
  cannot occur for on-board cells).
- `src/main/java/com/engine/nnue_trainer/v2/NNUEv2Evaluator.java` — the shape to mirror for
  loading: `DEFAULT_WEIGHTS` path constant, static `load(Path)`, instance `evaluate(...)`.
- `src/main/java/com/engine/nnue_trainer/search/gobot/GoBotSearcher.java` — the leaf wiring to
  mirror: `enum LeafEval { HAND_TUNED, NNUE, NNUEV2 }`, the immutable `LeafConfig` snapshot,
  `configureDefaultLeafEvalV2(mode, evaluator)`, `restoreDefaultLeafEval(prev)`, and the
  `leafEval(state)` branch at ~line 594 which queries from the leaf's own mover and negates to the
  root's perspective.
- `src/main/java/com/engine/nnue_trainer/search/SearchEngine.java` — the `EVAL=` env hook, the
  lazy `volatile` shared-evaluator load with a warn-once failure fallback, and `setUseNnueV2Eval` /
  `setNnueV2Evaluator` test hooks.
- `python/v3/fit_capacity.py` — the fitter whose predictions the Java runtime must reproduce.

Environment: Java 17 + Maven (`./mvnw`), Jackson, numpy available. **No new dependencies.**

## Development Approach

- **Testing approach**: Regular (code first, then tests).
- Complete each task fully before moving to the next.
- **CRITICAL: every task MUST include new/updated tests** for code changes in that task.
  - cover both success and edge/error scenarios
  - the load path needs malformed-input tests; the evaluator needs a **cross-language parity test**
    (see Task 3) — that is the one that actually proves correctness
- **CRITICAL: all tests must pass before starting the next task** — no exceptions.
- **CRITICAL: update this plan file when scope changes during implementation.**
- **Default OFF is non-negotiable.** With `EVAL` unset, play must be byte-identical to today's
  hand-tuned behavior, exactly as the v2 integration guarantees. Master auto-deploys to prod on
  merge, so a default-path regression here reaches the live bot.
- Do NOT touch NNUE v1 or v2 evaluation paths. Do NOT run a gauntlet here — that is `d4a.6.2`.
- Do NOT add incremental accumulator updates, a score-scale env knob, or quantization.

## Testing Strategy

- **Unit tests**: JUnit under `src/test/java/com/engine/nnue_trainer/v3/` (and `search/` for the
  leaf wiring), matching existing conventions.
- **Cross-language parity**: a committed fixture of boards with the Python fitter's predicted
  scores, asserted against the Java evaluator (Task 3). Mirrors the existing v2
  `accumulator_parity_fixture.json` approach, and is generated by a script so a refit can
  regenerate it rather than rot into a hardcoded number.
- **E2E**: none in this project.

## Progress Tracking

- Mark completed items with `[x]` immediately when done
- Add newly discovered tasks with ➕ prefix
- Document issues/blockers with ⚠️ prefix
- Update plan if implementation deviates from original scope
- Keep plan in sync with actual work done

## What Goes Where

- **Implementation Steps** (`[ ]` checkboxes): accumulator, evaluator, parity fixture, leaf wiring,
  env hook, benchmark, tests, docs.
- **Post-Completion** (no checkboxes): the gauntlet itself (`d4a.6.2`) and the owner's read of it.

## Implementation Steps

### Task 1: NNUEv3Accumulator — active-feature extraction

- [x] create `src/main/java/com/engine/nnue_trainer/v3/NNUEv3Accumulator.java` exposing the active
      feature indices for a `(board, stm)` pair: for each of the 144 cells, resolve the state via
      `PatternContract.getSymbol(board.getCell(r,c), stm)` and emit `idx(r,c,state)`
- [x] share the index definition with `V3FeatureMiner` rather than duplicating the arithmetic — a
      second copy of `(r*12+c)*8+state` is exactly how the runtime and the training data drift apart
      (thin delegation to `V3FeatureMiner.idx` / `activeFeatures`, plus a `FEATURES = 1152` constant)
- [x] guard board size explicitly: v3 is 12x12-only, so a non-12x12 board must be rejected by the
      accumulator (the caller decides the fallback, see Task 4) rather than silently mining the
      top-left 12x12
- [x] write a test asserting exactly 144 indices, all in `[0,1152)`, and no two sharing a cell
- [x] write a test asserting perspective symmetry: the same board from stm=1 and stm=2 maps
      self/opponent states consistently (a cell owned by 1 is `*_SELF` for stm=1 and `*_OPP` for
      stm=2)
- [x] write a test for the non-12x12 rejection
- [x] run tests - must pass before next task

### Task 2: NNUEv3Evaluator — load weights, evaluate

- [x] create `src/main/java/com/engine/nnue_trainer/v3/NNUEv3Evaluator.java` with a
      `DEFAULT_WEIGHTS = Path.of("nnue_v3_weights.json")` constant, a static `load(Path)`, and an
      instance `evaluate(Board board, int stm)` returning the score in **hand-tuned eval units**
      (returns `double`; the leaf rounds to its `long` frame in Task 4)
- [x] load into a flat `double[1152]` (or `float[]`) indexed by feature id, plus the `bias`; a
      missing index means weight 0
- [x] **validate on load** rather than deferring to an AIOOBE mid-search (the lesson from
      `PatternDictionary.load`): reject a missing/!object `weights` or `meta`, a non-finite weight
      or bias, and any index outside `[0, n_features_total)` (➕ also rejects
      `n_features_total != 1152` — a file fitted over a different feature space would otherwise load
      clean and evaluate nonsense — and non-integer feature-id keys)
- [x] evaluate as `bias + Σ weight[idx]` over the accumulator's active indices — no scaling, no
      squashing, no clamping beyond what the leaf needs for mate bounds
- [x] write tests: a known small weights file evaluates to a hand-computed score; malformed inputs
      (missing key, wrong type, NaN weight, out-of-range index) each throw on load
- [x] run tests - must pass before next task

### Task 3: Cross-language parity — Java must reproduce the Python fit

- [x] add a generator (e.g. `python/v3/gen_v3_eval_fixture.py`) that takes a handful of real 12x12
      boards, computes `eval_v3` with the same arithmetic the fitter uses, and writes
      `src/test/resources/v3/eval_parity_fixture.json` (board, stm, expected score) — predictions go
      through `fit_capacity.design(...)[:, :-1] @ w + bias`, the fitter's own line, not a re-derived sum
- [x] write the Java parity test asserting `NNUEv3Evaluator.evaluate` matches each fixture entry
      within a tight float tolerance (`V3EvalParityTest`, tol 1e-5 on scores up to ~3e4)
- [x] prefer boards drawn from the real corpus over synthetic ones — the point is to catch a
      perspective or index mismatch on positions the model will actually see (8 distinct boards
      strided over the mined positions JSONL; the generator skips repeats so the shared opening
      position is not sampled eight times, and asserts the board reconstruction round-trips)
- [x] this is the test that proves the runtime reproduces the fitted model; if it disagrees, the
      index mapping or the perspective normalization is wrong and nothing downstream is meaningful
      (each board is asserted from BOTH stm=1 and stm=2, so a self/opponent mix-up cannot pass)
- [x] ➕ `python/v3/gen_v3_eval_fixture_test.py`: recomputes every committed fixture score from
      `nnue_v3_weights.json`, so a refit that leaves the fixture stale fails in CI's Python job
      rather than as a mystery Java parity failure
- [x] run tests - must pass before next task (`./mvnw test` BUILD SUCCESS; 37 Python tests OK)

### Task 4: Wire the GoBot leaf and the EVAL=NNUEV3 env hook

- [ ] add `NNUEV3` to `GoBotSearcher.LeafEval` and carry an `NNUEv3Evaluator` on `LeafConfig`
      alongside the existing v2 slot; add `configureDefaultLeafEvalV3(mode, evaluator)` as a
      distinct name (not an overload) so existing callers stay unambiguous
- [ ] add the `leafEval(state)` branch mirroring the v2 one: query from the leaf's own mover, then
      negate to the root's perspective by zero-sum negation
- [ ] add the `EVAL=NNUEV3` branch in `SearchEngine`, mirroring the v2 lazy `volatile` shared load
      with a warn-once fallback when the weights fail to load
- [ ] **12x12 fallback**: when `EVAL=NNUEV3` is set but the board is not 12x12, fall back to the
      hand-tuned eval rather than throwing — the engine plays other sizes and must not crash
- [ ] write a test asserting `EVAL` unset leaves `LeafEval.HAND_TUNED` and produces identical moves
      to today (the default-OFF guarantee)
- [ ] write a test asserting `EVAL=NNUEV3` selects the v3 leaf, and one asserting the non-12x12
      fallback path
- [ ] write a test asserting a failed weights load falls back instead of propagating
- [ ] run tests - must pass before next task

### Task 5: Benchmark NPS against hand-tuned

- [ ] measure nodes/sec of the GoBot search with `LeafEval.NNUEV3` against `HAND_TUNED` on a fixed
      set of positions, and record the ratio in `docs/nnue-v3-runtime.md`
- [ ] state plainly whether full recompute is fast enough for the live 60k-node budget, since that
      is what decides whether incremental updates are ever needed (they are explicitly out of scope
      here)
- [ ] keep the benchmark off the default test path if it is slow (tag/skip it like the existing v2
      benchmark test)
- [ ] run tests - must pass before next task

### Task 6: Verify acceptance criteria

- [ ] verify the bead is covered: accumulator, evaluator, `NNUEV3` leaf, `EVAL=NNUEV3` hook, NPS
      benchmark
- [ ] verify NO gauntlet, NO training, NO incremental updates, NO scale knob were added
- [ ] verify the default-OFF guarantee explicitly: with `EVAL` unset the engine's chosen moves are
      unchanged
- [ ] verify edge cases: non-12x12 board, missing weights file, weights with fewer than 1152 entries
- [ ] run the full test suite (`./mvnw test` and the Python tests) — must pass
- [ ] run `./mvnw spotless:apply` — CI checks formatting before it runs tests

### Task 7: [Final] Update documentation

- [ ] write `docs/nnue-v3-runtime.md`: how to enable (`EVAL=NNUEV3`), where the weights come from,
      the NPS result, the 12x12 restriction and the fallback behavior
- [ ] state explicitly that v3 needs no score scale and why (fit against hand-tuned units), so the
      v2 `NNUEV2_SCALE` precedent is not cargo-culted forward

## Technical Details

**Score units**: hand-tuned eval units, directly. Holdout MAE was 1230 against labels spanning
roughly ±30000. No scaling.

**Perspective**: `evaluate(board, stm)` is STM-relative (positive = good for `stm`), matching both
`HandTunedEval.staticEval` and the training labels. The leaf negates to the root's frame exactly as
the v2 branch does.

**Board size**: 12x12 only. The accumulator rejects other sizes; the `SearchEngine` branch falls
back to hand-tuned so play on other sizes is unaffected.

**Weights file**: repo-root `nnue_v3_weights.json`, overridable by env (`NNUEV3_WEIGHTS`) in the
same spirit as `NNUEV2_WEIGHTS`.

## Post-Completion

*Items requiring manual intervention or external systems — no checkboxes, informational only*

- `d4a.6.2` runs the gauntlet: v3 leaf vs GoBot and vs the hand-tuned clone. The hand-tuned clone is
  6-0 against GoBot, so a v3 eval that genuinely matches it should also beat GoBot; a tie against
  hand-tuned is the expected outcome of a faithful distillation.
- Do not promote anything to the live bot on the strength of this bead. `EVAL` stays unset in prod
  until a gauntlet says otherwise.
