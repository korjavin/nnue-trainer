version: "2"
sql:
  - schema: "internal/migrations"
    queries: "sql/queries"
    engine: "sqlite"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
